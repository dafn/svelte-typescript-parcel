{
  "name": "svelte-typescript-parcel",
  "version": "1.0.0",
  "main": "src/index.ts",
  "license": "MIT",
  "scripts": {
    "serve": "rm -rf dist && parcel serve src/index.html",
    "build": "rm -rf dist && parcel build src/index.html"
  },
  "dependencies": {
    "svelte": "^3.20.1"
  },
  "devDependencies": {
    "node-sass": "^4.13.1",
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-svelte": "^4.0.6",
    "svelte-preprocess": "^3.7.01",
    "typescript": "^3.8.3"
  },
  "browserslist": [
    "last 1 chrome versions"
  ]
}
