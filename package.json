{
  "name": "svelte-typescript-parcel",
  "version": "1.0.0",
  "main": "src/index.ts",
  "license": "MIT",
  "scripts": {
    "serve": "rm -rf dist && parcel serve src/index.html",
    "build": "rm -rf dist && parcel build src/index.html"
  },
  "dependencies": {
    "svelte": "^3.9.1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.3",
    "parcel-plugin-svelte": "^4.0.2",
    "svelte-preprocess": "^3.1.0",
    "typescript": "^3.5.3"
  },
  "browserslist": [
    "last 1 chrome versions"
  ]
}
